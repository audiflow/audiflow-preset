interface:
  display_name: "audiflow Preset"
  short_description: "Create and manage preset patterns for audiflow"
  brand_color: "#6366F1"
  default_prompt: "Create a new audiflow preset pattern"
  instructions_file: "../SKILL.md"
  related_paths:
    - "../SKILL.md"
    - "../references/schema-reference.md"
    - "../scripts/search-feeds.py"
    - "../scripts/analyze-feed.py"
policy:
  allow_implicit_invocation: true
